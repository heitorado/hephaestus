import React from 'react';
import { Text, View } from 'react-native';
import styles from './styles';

const SavedItemsScreen = () => (
  <View style={styles.container}>
    <Text>I am the Saved Items screen.</Text>
  </View>
);

export default SavedItemsScreen;
